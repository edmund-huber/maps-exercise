To run locally, first make a postgres user and give it access to a
fresh database. Then pipe `scaffold.sql` into `psql -d
<database-name>`.

To run under bottlepy's development server,

DATABASE_URL=your-postgres-url python web.py